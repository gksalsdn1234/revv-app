# 2026-09-07 작업 기록 및 현재 상태

> 최신 9월 8일 후속: [리뷰 문제 7개 수정 및 전체 698개 검증](2026-09-08-review-fixes.md)을 완료했다.

> 9월 8일 후속: 저장·복구, 시작·종료, 권한·GPS 상태의 코드 수정과 검증을 완료했다. 최신 결과와 실기기 확인 범위는 [출시 전 신뢰성 수정](2026-09-08-pre-release-reliability.md)을 확인한다. 아래 감사 결과는 수정 전 이력이다.

기준: 2026-09-07 KST. 활성 작업 폴더는 `/Users/minwoohan/Documents/revv-app-release-integration`, 브랜치는 `codex/revv-guideline4-language`다. 오늘 작업 시작 커밋은 `9c60dc6`. 과거 제출 문서와 측정 기록은 당시 이력이며, 현재 개발·배포 상태는 이 문서를 먼저 확인한다.

## 현재 반영 상태

| 대상 | 확인된 상태 |
| --- | --- |
| 운영 API | REVV 프로젝트 `zvwgnduuumksuqazpvsf`에 overview 경량화와 catalog 조회 개선 마이그레이션 모두 배포 완료 |
| 앱 소스 | 경량 geometry 수신, 선택 시 원본 geometry 조회, 요청 병렬화, 지도 갱신 개선 구현·검증 완료 |
| iOS 아이콘 | 8월 9일 최신 아이콘의 PNG 15개 복원, 시뮬레이터 설치·표시 확인 |
| 앱 버전 | 소스 `1.38.0+63` 유지. 오늘 앱/아이콘 변경을 App Store에 업로드하지 않음 |
| 이전 제출 이력 | 9월 1일 build 63 업로드·재심사 제출 및 당시 심사 대기 상태 확인. 사용자는 이후 출시했다고 설명함. 현재 심사/판매 상태를 오늘 작업에서 독립적으로 재확인한 기록은 없음 |
| 다운로드 | 검증 가능한 다운로드·재사용 수치가 이 작업 기록에 없음. 데이터 미확인을 0건으로 기록하지 않음 |
| GitHub 보관 브랜치 | [`codex/revv-guideline4-language`](https://github.com/gksalsdn1234/revv-app/tree/codex/revv-guideline4-language). 언어 수정 `2dbb2a2`, 성능·아이콘 `658a1d7`, 이 문서는 후속 문서 커밋에 포함. 최종 원격 HEAD 검증 결과는 공유 인수인계에 기록 |

## 오늘 한 일

### 제품 맥락과 기존 자료 재검토

출시 계획, 마케팅 전략, 몬트리올 루트 감사, RoadCurvature 처리 코드와 Western OSM 생성·발행 자료를 읽었다. 사용자는 한국으로 돌아왔으며, 다른 사람들이 루트 자체가 재미있다고 평가했다고 설명했다. 목표는 일부 애호가만 아는 굽은 길의 발견을 쉽게 만드는 것이다. 루트 품질에 대한 정성 피드백과 설치·재사용 지표는 별개로 다룬다.

초기 화면에서 1–3개만 추천하기보다 여러 루트를 보여주고 싶다는 사용자 의도를 유지했다. 이번 최적화는 주변 120개·카탈로그 650개 상한을 줄이지 않았다. 전체 카탈로그가 전부 수작업 큐레이션이거나 독점 원천이라는 주장은 근거가 없어 사용하지 않는다. 한국 공략과 몬트리올 원격 운영은 기존 전략의 맥락이며, 현재 한국 루트 커버리지가 검증됐다는 의미는 아니다.

### 시작 → 주행 → 리포트 흐름 감사

코드와 임시 재현 테스트로 다음을 확인했다. 이는 감사 결과이며 이번 성능 작업으로 모두 수정됐다고 간주하지 않는다.

- 희소 루트 원본 좌표를 기다리는 동안 종료할 수 있어 세션 없는 리포트로 연결되는 경로.
- 수동 시작 시 백그라운드 위치 추적과 자동 기록 설명의 불일치. 실기기 중단 시험은 미수행.
- 저장 실패가 성공처럼 보이는 체크 표시와 `No session`으로 표현되고 재시도 수단이 부족한 문제.
- 복구 시 요약만 저장한 뒤 스냅샷을 지워 GPS·텔레메트리 상세가 소실되는 경로.
- 클라우드 동기화 동의 시 로컬 저장 결과 반환이 상세 업로드 대기에 묶이는 경로.

기존 관련 테스트 78개와 임시 재현 3개 통과. 임시 테스트는 제품 테스트 디렉터리에서 제거했고 원본은 `/private/tmp/revv-drive-flow-audit-20260907/`에 있다. 원본 geometry hydration 경로가 오늘 변경됐으므로 시작·종료 경합은 다음 수정 시 최신 코드로 다시 재현해야 한다.

### 첫 루트 발견 흐름 감사

권한 미결정을 꺼짐으로 표시하는 상태, 첫 GPS 대기를 루트 로딩 실패/Retry로 표시하는 상태, shell과 finder의 초기 위치 권한 요청 중복을 재현했다. 위치 없이 지역 검색이 가능하지만 안내가 충분하지 않다. 지도 초기 카메라가 몬트리올 도심에 가깝게 잡혀 루트 데이터가 준비돼도 유용한 와인딩 선이 바로 보이지 않을 수 있음을 시뮬레이터에서 관찰했다.

기존 관련 테스트 60개와 임시 감사 테스트 4개 통과. 원본은 `/private/tmp/revv-first-route-audit-20260907/`에 있다. 권한 UI/초기 카메라 개선은 미완료이며, 이번 작업은 루트 수를 줄이는 화면 개편을 하지 않았다.

### 루트 전송·지도 처리 개선

- 위치, epoch, RPC 헤더/본문, 변환, 캐시, 지도 source/frame 단계 측정 추가.
- 표시용 경량 좌표와 주행용 원본 좌표를 분리하고 선택·주행 준비 시 원본 조회. 복합 루트는 구성 루트의 원본으로 재조립.
- epoch와 주변 조회를 겹쳐 실행하면서 발행 검증 유지. 캐시 저장 완료 전에 화면에 결과 전달.
- 지도 source/layer 재사용 및 대기 중인 중간 지도 갱신 생략.
- `20260907120251_route_lightweight_overview.sql` 운영 배포.
- `20260907131716_route_catalog_ordinal_lookup.sql` 운영 배포. ID 목록의 반복 처리·정렬과 중간 전체 좌표 결과 생성을 줄여 catalog timeout 병목 해결.

운영 데이터 자체, 전역 DB 설정, 8초 함수 제한은 바꾸지 않았다. 인증·발행 가시성·중복 제거·입력 순서를 보존했다. 보안 advisor는 두 번째 배포 전후 38개로 동일하며, 앞선 overview 배포에서 생긴 의도된 authenticated SECURITY DEFINER 알림 2개는 검토해 상세 문서에 설명했다.

### 최신 iOS 아이콘 복원 및 도구 지침 정리

`revv-consolidate`의 `ac99fc171d157d1a50e12e46f155e9dd24a564c3`에 있던 검정 바탕·빨간 타코미터·흰 도로 아이콘을 복원했다. PNG 15개가 해당 커밋과 바이트 단위 일치하고 크기·불투명 RGB 형식이 올바름을 검증했다. 시뮬레이터 홈 아이콘과 앱 실행을 확인했다.

사용자 요청에 따라 보존용 `revv-app/AGENTS.md`의 Lazyweb router와 Codex Lazyweb 스킬 등록을 제거했다. 이는 전역/보존 작업공간 변경으로, 활성 앱 브랜치의 푸시에 포함되지 않는다. 활성 작업공간 AGENTS.md에는 이미 Lazyweb 지침이 없다.

## 최종 측정과 검증

iPhone 16e / iOS 26.3 시뮬레이터, Debug, 몬트리올 고정 위치. 첫 실행은 로그인·설정을 유지하고 루트 캐시만 비웠고 이후 두 번은 캐시를 유지했다.

| 항목 | 루트 캐시 없음 | 캐시 재실행 1 | 캐시 재실행 2 |
| --- | ---: | ---: | ---: |
| 전체 catalog 응답 | 200 / 1.558초 | 200 / 2.573초 | 200 / 1.524초 |
| 주변 루트 조회 | 7.960초 | 4.820초 | 2.545초 |
| 실행 요청 → 첫 nonempty source 적용 후 frame | 10.768초 | 3.071초 | 2.870초 |

전체 catalog는 직전 측정에서 3회 중 2회 timeout이었으며 이번에는 3회 모두 성공했다. 주변 응답 본문은 1,200,754 → 415,062 bytes(65.4% 감소), catalog는 3,283,708 → 817,859 bytes(75.1% 감소). 이는 압축 전 수신 본문 크기이며, 최초 설치와 로그인 유지 캐시 초기화는 동일 조건이 아니다. 세 번의 Debug 측정은 실기기 지연 분포나 모든 로딩 문제 해결을 보장하지 않는다.

앱 전체 테스트 672개 통과·기존 조건부 skip 2개, analyze 오류 없음, Debug iOS 빌드 성공. 이후 catalog SQL 수정의 관련 테스트 68개와 transaction-local SQL fixture 통과. 마지막으로 Route Promenade 상세 geometry/57 km/89분 표시를 확인했으며 실제 주행은 시작하지 않았다. 문서/커밋 정리에서는 제품 코드를 바꾸지 않아 이미 통과한 전체 테스트를 반복하지 않는다.

세부 변경·SQL 검증·배포 hash·측정 원본 경로: [루트 성능 기록](2026-09-07-route-delivery-performance.md). `/private/tmp` 증거는 임시 파일이므로 지속 기록은 이 문서의 수치와 저장소의 SQL/test 코드를 기준으로 한다.

## 다음 작업

1. **주변 루트 조회 지연**: 아직 2.5–8초 편차가 있다. 실제 쿼리 계획·헤더 대기·네트워크를 나눠 측정해 첫 캐시 없는 진입을 개선한다.
2. **권한/GPS와 최초 지도 범위**: 권한 요청을 통합하고 대기·거부·실패 상태를 분리한다. 여러 루트가 있다는 장점을 첫 화면에서 확인할 수 있는지 검증한다.
3. **주행·저장 신뢰성**: 최신 코드에서 시작/종료 경합을 재현하고, 저장 실패 재시도·복구 상세 보존·백그라운드 추적을 수정·검증한다.
4. **실기기와 앱 릴리즈**: Profile 빌드로 캐시 없음/재실행/선택/시작을 확인한다. 오늘 소스·아이콘을 배포하려면 별도 새 빌드 번호와 App Store 업로드가 필요하다. 이미 업로드한 63번을 새 바이너리에 재사용하지 않는다.
5. **첫 설치 행동 측정**: 검색 노출·다운로드 통계를 기간/지역과 함께 확인하고, 루트 공유 → 설치 → 첫 미리보기 → 실제 주행을 측정한다. 지표를 확보하기 전 대규모 광고 효과를 단정하지 않는다.
