# 출시 전 남은 수동 항목 (2026-07-07 캠페인 종료 시점)

자동화 캠페인(G001~G005)으로 코드 쪽 범위 잠금은 완료. 아래는 민우 판단/손이 필요한 것만.

## 릴리즈 블로커
1. **beep.mp3 교체** — 현재 F1 방송 클립(라이선스 없음). 제출 전 반드시 교체.
   원클릭 대안: `git show b8abd21:assets/sounds/beep.mp3 > assets/sounds/beep.mp3` (오리지널 합성음, 0.31s). 민우가 "지금은 유지" 결정(07-06) — 제출 직전 재확인 필요.
2. **push** — 로컬 커밋 다수 미푸시. 승인 시 `git push origin lean_mvp`.

## 실기기 확인 (코드 완료, 체감 검증만 남음)
- 플래너: 빨간 와인딩 선 표시 문제 — 민우 보고("아예 안 보임") 후 스크린샷 미수신. 코드상 이상 없음 확인됨 → 새 빌드에서 재확인 필요
- 생추천형: "그냥 추천받아 달리기" 루프 품질 (실주행 검증 원칙 — 크루와 직접 달려보기)
- 워키: LTE/터널 재연결, ack:false 음질, 250ms 프리버퍼 체감
- 기본vs REVV 비교 라인, 커브 칩 표기
- 학습루프: 실기기에서 추천/선택 후 Supabase recommendation_logs에 행 생기는지 (Table Editor에서 확인)

## 심사 제출 준비 (7/11 게이트, launch-plan Week2)
- 스크린샷 (실앱과 일치해야 — 리젝 포인트)
- 지원 URL / 연령 등급 / 심사 노트 (위치 권한 사유)
- 개인정보 라벨 (Supabase 익명 인증 + 위치 수집 명시)
- `flutter build ipa --release --dart-define-from-file=.env` (REVV_WALKIE_LAB **제외** — 워키는 V2, 심사 빌드에서 랩 플래그 끄기)
